Teach4Free - Manual Testing Notes
=================================

How to run tests manually
-------------------------

Install dependencies:

- composer install
- npm install

Prepare the local environment:

- copy .env.example .env
- php artisan key:generate
- Create a MySQL/MariaDB database named teach4free.
- php artisan migrate --seed

Useful checks:

- php artisan test
- composer run lint:check
- npm run lint:check
- npm run types:check
- npm run build

Run the application locally:

- composer run dev

Then open:

- http://127.0.0.1:8000

Test log - 2026-05-10
---------------------

Command:

- php artisan test --filter='PublicPagesTest|DashboardTest|AdminDashboardTest|LocaleTest'

Initial result:

- Failed.
- 8 tests executed.
- 4 passed.
- 4 failed.
- Error: Vite manifest not found at public/build/manifest.json during Inertia page rendering.

Fix applied:

- Updated tests/TestCase.php to call withoutVite() during tests.

Command:

- php artisan test --filter='PublicPagesTest|DashboardTest|AdminDashboardTest|LocaleTest'

Result:

- Passed.
- 8 tests executed.
- 8 passed.
- 14 assertions.

Command:

- npm install

Result:

- Passed.
- 489 packages installed.
- 0 vulnerabilities reported.

Command:

- npm run types:check

Initial result:

- Failed.
- TypeScript reported auth.user could be null in resources/js/pages/settings/profile.tsx.

Fix applied:

- Narrowed the authenticated user into a local user constant after the null guard.

Command:

- npm run types:check

Result:

- Passed.

Command:

- composer run lint:check

Initial result:

- Failed.
- Pint reported import ordering in bootstrap/app.php.

Fix applied:

- Reordered imports in bootstrap/app.php.

Command:

- composer run lint:check

Result:

- Passed.

Command:

- npm run lint:check

Initial result:

- Failed.
- ESLint reported import ordering issues in five TypeScript/TSX files.

Fix applied:

- Reordered imports in the reported files.

Command:

- npm run lint:check

Result:

- Passed.

Command:

- npm run build

Result:

- Passed.
- Vite production build completed.
- Note: Vite printed plugin timing warnings, but the build succeeded.

Command:

- npm run lint:check

Result:

- Passed.
- ESLint reported no errors.

Command:

- composer run lint:check

Initial result:

- Failed.
- Pint reported ordered imports needed in routes/web.php.

Fix applied:

- Reordered the admin controller imports in routes/web.php.

Command:

- composer run lint:check

Result:

- Passed.

Command:

- php artisan test

Result:

- Passed.
- 46 tests executed.
- 46 passed.
- 147 assertions.

Additional local setup/verification
-----------------------------------

Command:

- mysql -u root -e "CREATE DATABASE IF NOT EXISTS teach4free CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

Result:

- Passed.

Command:

- php artisan migrate --seed

Result:

- Passed.
- Initial migrations ran.
- Development seed users were created.

Command:

- Invoke-WebRequest http://127.0.0.1:8000

Result:

- Passed.
- HTTP status 200.

Profile, language and Google login task - 2026-05-10
----------------------------------------------------

Command:

- php artisan test --filter='ProfileSetupTest|AdminProfileCountsTest|GoogleAuthenticationTest'

Result:

- Passed.
- 13 tests executed.
- 13 passed.
- 59 assertions.

Command:

- npm run types:check

Result:

- Passed.
- TypeScript reported no errors.

Command:

- npm run lint:check

Initial result:

- Failed.
- ESLint reported type-only import issues in:
- resources/js/pages/profile/preferences.tsx
- resources/js/pages/profile/student.tsx
- resources/js/pages/profile/teacher.tsx

Fix applied:

- Ran npm run lint to apply the project's automatic ESLint fixes.

Command:

- composer run lint:check

Initial result:

- Failed.
- Pint reported import ordering issues in:
- app/Models/Language.php
- app/Models/StudentProfile.php
- app/Models/TeacherProfile.php
- app/Models/UserLanguage.php

Fix applied:

- Ran composer run lint to apply the project's automatic Pint fixes.

Command:

- npm run lint:check

Result:

- Passed.

Command:

- composer run lint:check

Result:

- Passed.

Command:

- npm run build

Result:

- Passed.
- Vite production build completed.
- Note: Vite printed plugin timing warnings, but the build succeeded.

Command:

- php artisan test

Result:

- Passed.
- 59 tests executed.
- 59 passed.
- 206 assertions.

Command:

- php artisan migrate --seed

Result:

- Passed.
- The new profile, language and Google user field migrations ran.
- The development seeder completed.

Command:

- Invoke-WebRequest http://127.0.0.1:8000
- Invoke-WebRequest http://127.0.0.1:8000/about
- Invoke-WebRequest http://127.0.0.1:8000/login

Result:

- Passed.
- Each request returned HTTP status 200.

First product block: avatars, admin catalog and teaching offers - 2026-05-10
----------------------------------------------------------------------------

Command:

- php artisan route:list --path=offers
- php artisan route:list --path=teacher/offers
- php artisan route:list --path=admin

Result:

- Passed.
- Public offer routes, teacher offer routes and admin routes were registered.

Command:

- php artisan test --filter='TeachingOffersTest|AdminCatalogTest|AvatarUploadTest'

Result:

- Passed.
- 13 tests executed.
- 13 passed.
- 92 assertions.

Command:

- npm run types:check

Result:

- Passed.
- TypeScript reported no errors.

Command:

- npm run lint:check

Initial result:

- Failed.
- ESLint reported formatting/import ordering issues in the new React pages.
- ESLint also reported one unused icon import in resources/js/pages/offers/index.tsx.

Fix applied:

- Ran npm run lint for automatic fixes.
- Removed the unused GraduationCap import manually.

Command:

- composer run lint:check

Initial result:

- Failed.
- Pint reported import/style issues in routes, controllers and the seeder.

Fix applied:

- Ran composer run lint to apply the project's automatic Pint fixes.

Command:

- npm run lint:check

Result:

- Passed.

Command:

- composer run lint:check

Result:

- Passed.

Command:

- npm run types:check

Result:

- Passed.

Command:

- php artisan migrate --seed

Result:

- Passed.
- The avatar, teaching category, teaching subject, teaching offer and offer language migrations ran.
- The development seeder completed and created demo catalog/offer data.

Command:

- php artisan test

Result:

- Passed.
- 72 tests executed.
- 72 passed.
- 298 assertions.

Command:

- npm run build

Result:

- Passed.
- Vite production build completed.
- Note: Vite printed plugin timing warnings, but the build succeeded.

Command:

- Invoke-WebRequest http://127.0.0.1:8000/offers
- Invoke-WebRequest "http://127.0.0.1:8000/offers?category=programming&language=en&level=beginner"
- Invoke-WebRequest http://127.0.0.1:8000/offers/laravel-for-beginners

Result:

- Passed.
- Each public request returned HTTP status 200.

Command:

- Invoke-WebRequest http://127.0.0.1:8000/teacher/offers -MaximumRedirection 0

Result:

- Passed.
- Guest access returned HTTP status 302, redirecting to authentication as expected.

Application flow and notification task
======================================

Manual test instructions
------------------------

Run backend tests manually with:

- php artisan test

Run frontend type checks manually with:

- npm run types:check

Run the frontend production build manually with:

- npm run build

Useful browser checks:

- Register a new account and confirm that email verification is requested.
- Log in as user@example.com / password.
- Open /offers/laravel-for-beginners and apply from a verified non-owner account.
- Open /teacher/applications as the teacher account to accept or reject requests.
- Open /my-applications as the student account to view or cancel applications.
- Open /notifications and confirm unread/read behavior.
- Log in as admin@example.com / password and open /admin/applications.

Test log
--------

Command:

- php artisan route:list --except-vendor

Result:

- Passed.
- Route list rendered successfully.
- New routes for applications, teacher requests, notifications and admin applications were present.

Command:

- php artisan about --only=environment

Result:

- Passed.
- Laravel environment information rendered successfully.

Command:

- php artisan test tests\Feature\TeachingOfferApplicationsTest.php

Result:

- Passed.
- 13 tests executed.
- 13 passed.
- 85 assertions.

Command:

- php artisan test tests\Feature\Auth\RegistrationTest.php tests\Feature\Auth\EmailVerificationTest.php tests\Feature\Auth\VerificationNotificationTest.php tests\Feature\Auth\GoogleAuthenticationTest.php tests\Feature\TeachingOffersTest.php tests\Feature\DashboardTest.php tests\Feature\AdminDashboardTest.php tests\Feature\AdminProfileCountsTest.php

Result:

- Passed.
- 26 tests executed.
- 26 passed.
- 117 assertions.

Command:

- npm run types:check

Result:

- Passed.
- TypeScript reported no errors.

Command:

- npm run build

Result:

- Passed.
- Vite production build completed.
- Note: Vite printed plugin timing warnings, but the build succeeded.

Command:

- php artisan test

Result:

- Passed.
- 85 tests executed.
- 85 passed.
- 385 assertions.

Command:

- php artisan test --filter=RegistrationTest

Result:

- Passed.
- 2 tests executed.
- 2 passed.
- 6 assertions.

Global navigation, appearance and avatar polish - 2026-05-11
------------------------------------------------------------

Command:

- npm run types:check

Result:

- Passed.
- TypeScript reported no errors.

Command:

- php artisan test tests\Feature\AvatarUploadTest.php tests\Feature\NavigationExperienceTest.php

Result:

- Passed.
- 9 tests executed.
- 9 passed.
- 71 assertions.

Command:

- composer run lint:check

Result:

- Passed.
- Pint reported no required changes.

Command:

- composer run lint:check

Result:

- Passed.
- Pint still reported no required changes after the storage URL refinement.

Command:

- npm run lint:check

Initial result:

- Failed.
- ESLint reported import ordering in resources/js/layouts/auth/auth-simple-layout.tsx.

Fix applied:

- Reordered the new AppearanceToggle import after AppLogoIcon.

Command:

- npm run lint:check

Result:

- Passed.
- ESLint reported no errors.

Command:

- php artisan test

Result:

- Passed.
- 92 tests executed.
- 92 passed.
- 440 assertions.

Global navigation/avatar follow-up audit - 2026-05-11
-----------------------------------------------------

Command:

- php artisan test tests\Feature\AvatarUploadTest.php tests\Feature\NavigationExperienceTest.php

Result:

- Passed.
- 10 tests executed.
- 10 passed.
- 83 assertions.

Command:

- npm run types:check

Result:

- Passed.
- TypeScript reported no errors.

Command:

- composer run lint:check

Result:

- Passed.
- Pint reported no required changes.

Command:

- npm run lint:check

Result:

- Passed.
- ESLint reported no errors.

Command:

- php artisan test

Result:

- Passed.
- 93 tests executed.
- 93 passed.
- 452 assertions.

Command:

- npm run build

Result:

- Passed.
- Vite production build completed.
- Note: Vite printed plugin timing warnings, but the build succeeded.

Command:

- npm run build

Result:

- Passed.
- Vite production build completed.
- Note: Vite printed plugin timing warnings, but the build succeeded.

Command:

- php artisan test tests\Feature\AvatarUploadTest.php

Result:

- Passed.
- 5 tests executed.
- 5 passed.
- 48 assertions.

Command:

- php artisan test

Result:

- Passed.
- 92 tests executed.
- 92 passed.
- 440 assertions.

Profile photo display fix - 2026-05-11
--------------------------------------

Tests were intentionally not run in this task because this task is focused on fixing profile photo display manually.

Manual verification required:

1. Upload photo in preferences.
2. Confirm it appears in preferences.
3. Confirm it appears in navbar.
4. Refresh page.
5. Confirm it still appears.
6. Replace photo.
7. Confirm replacement appears.
8. Remove photo if available.

Storage setup performed:

- php artisan storage:link

Result:

- public/storage is connected to storage/app/public.

Professional admin area and demo data - 2026-05-11
--------------------------------------------------

Tests were not executed in this task.

Reason:

- The task was a broad admin-area implementation and demo-data update.
- Automated tests were not run automatically.

Sanity checks executed, not automated tests:

- php -l on changed PHP controllers, middleware, models, seeder and routes.
- Result: Passed. No PHP syntax errors were reported.
- npm run types:check.
- Result: Passed. TypeScript reported no errors.
- php artisan route:list --path=admin and php artisan route:list --path=support/report.
- Result: Passed. New admin and support report routes were registered.
- composer run lint:check.
- Initial result: Failed. Pint reported ordered imports in app/Providers/FortifyServiceProvider.php and routes/web.php.
- Fix applied: composer run lint.
- composer run lint:check.
- Result: Passed. Pint reported no required changes.
- npm run lint:check.
- Initial result: Failed. ESLint reported import ordering and padding-line formatting in admin TSX files.
- Fix applied: npm run lint.
- npm run lint:check.
- Result: Passed. ESLint reported no errors.

Recommended manual verification:

1. Run php artisan migrate --seed.
2. Log in as admin@example.com / password.
3. Confirm login lands on /admin.
4. Confirm the admin dashboard does not show normal user/student/teacher dashboard cards.
5. Confirm the admin sidebar shows grouped admin sections.
6. Open /admin/users and test search/filter controls.
7. Open a normal user detail page, update basic fields, and verify ban/block controls are protected for admins.
8. Start impersonation for a normal user and confirm the banner appears.
9. Stop impersonation and confirm the admin account is restored.
10. Open /admin/incidents and update an incident status or priority.
11. Open /admin/languages and create or edit a language record.
12. Open /admin/world-map and confirm aggregate country data is shown.
13. Open a placeholder admin section and confirm it renders intentional content.
14. Open a public offer and confirm the report action opens /support/report.

Admin recovery/fix task - 2026-05-11
------------------------------------

Automated tests were not run in this task.

Reason:

- This was a recovery task focused on fixing the schema/login error, completing the admin-area foundation and reseeding demo data.
- Manual verification is still required for the browser flows below.

Commands executed:

- php -l database\migrations\2026_05_11_100000_add_admin_status_and_location_fields_to_users_table.php; php -l database\migrations\2026_05_11_100001_create_incidents_table.php; php -l database\migrations\2026_05_11_100002_create_audit_logs_table.php
- Result: Passed. No PHP syntax errors.

- php -l app\Http\Controllers\AdminDashboardController.php; php -l app\Http\Controllers\Admin\UserController.php; php -l app\Http\Controllers\Admin\WorldMapController.php; php -l app\Http\Controllers\SupportReportController.php; php -l app\Http\Responses\LoginResponse.php; php -l database\seeders\DatabaseSeeder.php; php -l routes\web.php
- Result: Passed. No PHP syntax errors.

- php -l lang\en\ui.php; php -l lang\es\ui.php; php -l lang\fr\ui.php
- Result: Passed. No PHP syntax errors.

- php artisan route:list --path=admin
- Result: Passed. Admin routes rendered, including /admin, /admin/dashboard, /admin/users, /admin/world-map and /admin/incidents.

- php artisan route:list --path=support/report
- Result: Passed. Support report routes rendered.

- php artisan migrate --seed
- Result: Passed. The last_login_at and last_login_ip migration ran, incidents and audit logs migrations ran, and the seeder completed.

- php artisan tinker --execute="echo 'users=' . \App\Models\User::count() . ' students=' . \App\Models\StudentProfile::count() . ' teachers=' . \App\Models\TeacherProfile::count() . ' mixed=' . \App\Models\User::whereHas('studentProfile')->whereHas('teacherProfile')->count() . ' offers=' . \App\Models\TeachingOffer::count() . ' applications=' . \App\Models\TeachingOfferApplication::count() . ' incidents=' . \App\Models\Incident::count();"
- Result: Passed. users=59 students=39 teachers=33 mixed=13 offers=53 applications=103 incidents=15.

- php artisan tinker --execute="echo 'last_login_at=' . (\Illuminate\Support\Facades\Schema::hasColumn('users', 'last_login_at') ? 'yes' : 'no') . ' last_login_ip=' . (\Illuminate\Support\Facades\Schema::hasColumn('users', 'last_login_ip') ? 'yes' : 'no') . ' countries=' . \App\Models\User::whereNotNull('country_code')->distinct()->count('country_code');"
- Result: Passed. last_login_at=yes last_login_ip=yes countries=12.

- php artisan tinker --execute="echo 'admin=' . (\App\Models\User::where('email', 'admin@example.com')->exists() ? 'yes' : 'no') . ' user=' . (\App\Models\User::where('email', 'user@example.com')->exists() ? 'yes' : 'no') . ' teacher1=' . (\App\Models\User::where('email', 'teacher1@example.com')->exists() ? 'yes' : 'no');"
- Result: Passed. admin=yes user=yes teacher1=yes.

- npm run types:check
- Result: Passed. TypeScript reported no errors.

- npm run lint:check
- Result: Passed. ESLint reported no errors.

- composer run lint:check
- Initial result: Failed. Pint reported unused imports in app\Http\Controllers\SupportReportController.php after validation was changed to use table names.
- Fix applied: removed unused imports.

- composer run lint:check
- Result: Passed. Pint reported no required changes.

Note:

- An initial tinker count command failed because of PowerShell escaping. It was rerun with corrected escaping and passed as recorded above.

Manual verification checklist:

1. Run:
   php artisan migrate:fresh --seed

2. Login as:
   admin@example.com
   password

3. Confirm admin lands on admin dashboard.

4. Confirm admin dashboard does NOT show:
   - My learning
   - My teaching
   - Learning profile
   - Teaching profile
   - My applications
   - Favorite teachers

5. Confirm admin dashboard shows platform statistics.

6. Confirm admin sidebar has administration sections.

7. Open /admin/users and confirm many users exist.

8. Open a demo teacher user detail.

9. Confirm ban/block actions are visible.

10. Confirm admin cannot ban/block self.

11. Open /admin/world-map and confirm multiple countries appear.

12. Open /admin/incidents and confirm demo incidents exist.

13. Login as teacher1@example.com / password.

14. Confirm no SQL error with last_login_at.

15. Confirm teacher/user dashboard still works.

16. Open /offers and confirm many demo offers exist.

17. Confirm public search looks populated.
